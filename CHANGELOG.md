# Changelog

## 0.1.0

- Added transcript extraction CLI with `extract`, `export`, and `aggregate` commands.
- Added `src/phrasify` package layout.
- Added JSONL, JSON, CSV, and Notion handoff exporters.
- Added stdlib `unittest` coverage for loader, chunking, schema mapping, export, LLM parsing, and aggregate paths.

